# .env — managed by Ansible
DATABASE_URL=postgresql://{{ postgres_user }}:{{ vault_umami_postgres_password }}@{{ postgres_host }}:{{ postgres_port }}/{{ postgres_db }}
DATABASE_TYPE=postgresql
APP_SECRET={{ vault_umami_app_secret }}
