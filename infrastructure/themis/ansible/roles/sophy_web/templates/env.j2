# {{ ansible_managed }}
# Credentials for sophy-web (systemd EnvironmentFile). Same panel user as
# sophy-switch on the laptop; the Themis host talks to itself by DNS name.
THEMIS_URL=https://{{ themis_domain }}
THEMIS_USER={{ headwind_api_user }}
THEMIS_PASSWORD={{ headwind_api_password }}
