# roles/hermes/handlers/main.yml - Service Restart Handlers

- name: restart hermes service
  systemd:
    name: hermes
    state: restarted
  listen: "restart hermes service"
  when: hermes_deploy_service | default(false)
