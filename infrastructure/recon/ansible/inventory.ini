# inventory.ini - Ansible Host Inventory for the recon detonation box.
# VM user is 'debian' (cloud-init default). Reachable only from the wired
# VLAN 10 workstation per the pfSense recon rule.

[recon]
recon-01 ansible_host=10.0.66.10 ansible_user=debian ansible_port=22

[recon:vars]
ansible_ssh_private_key_file = ~/.ssh/id_rsa
ansible_ssh_common_args = '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
ansible_python_interpreter = /usr/bin/python3
ansible_connection_timeout = 30
ansible_become = true
ansible_become_user = root
ansible_become_method = sudo
